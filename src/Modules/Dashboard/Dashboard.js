const checkAuth = require("../Auth/checkToken");
const fs = require('fs');
const path = require('path');

function Dashboard(){
    this.dashboard = {}
    const {client, io, dashboard} = this;

    
}


module.exports = Dashboard;